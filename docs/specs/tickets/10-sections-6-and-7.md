# 10: Sections 6 and 7 — the block in full, and the handoff to the demo

**What to build:** The frontmatter block reaches its full form, and the talk hands off to the live demo.

Section 6 grows the same block to the complete field set and annotates each field with **who supplies it** — not what it is. A machine-signed field and a human-signed field sit adjacent, identical in shape, differing only in actor, which does the argument's work without a schema lecture. One slide, no deep dive, with a link out to the public OKF specification for the curious.

Section 7 is the pivot: one line stating that the signal is in the file but the instruction has to come from the operator, with the literal configured sentence shown beneath it as the concrete instance. The audience reads the operator's own sentence before the demo, so when the demo echoes it they recognise where it came from.

**Blocked by:** 09 (grows that ticket's frontmatter block component).

**Status:** ready-for-agent

- [ ] Section 6 reveals the complete field set on the same block component from ticket 09, not a new block
- [ ] The block includes both the machine-generated and human-verified fields, adjacent and identical in shape, differing only in actor
- [ ] The human-verified field's actor uses the human-prefixed convention
- [ ] Each field carries a short annotation, indented to the right, stating who supplies it rather than what it is
- [ ] Annotations activate with their field via a field-highlight interaction
- [ ] A link to the public OKF specification is present and resolves
- [ ] Section 6 stays one slide with no deep dive into the schema
- [ ] Section 7 shows the headline: the signal is in the file, the instruction has to come from you
- [ ] Section 7 shows the literal configured sentence beneath it — the serious instance proposing that a ticket be opened, not the pirate variant, which is a live-demo device only
- [ ] Both sections are legible at typical screen-share scale and with motion disabled
- [ ] Archgate checks, eslint and prettier pass
